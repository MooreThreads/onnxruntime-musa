[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "@package_name@"
version = "@version@"
description = "ONNX Runtime MUSA Plugin Execution Provider"
readme = "onnxruntime_musa/README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
  # ABI is pinned to the vendored ORT public headers (third_party/onnxruntime/VERSION).
  "onnxruntime~=@onnxruntime_version@",
]

[tool.setuptools.packages.find]
include = ["onnxruntime_musa*"]

[tool.setuptools.package-data]
onnxruntime_musa = ["*.dll", "*.so", "*.so.*"]
